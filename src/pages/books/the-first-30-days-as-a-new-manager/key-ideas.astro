---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const base = import.meta.env.BASE_URL;
const href = (url) => {
  if (url.startsWith('http')) return url;
  const cleanBase = base.endsWith('/') ? base.slice(0, -1) : base;
  const cleanUrl = url.startsWith('/') ? url : `/${url}`;
  return `${cleanBase}${cleanUrl}`;
};


const bookUrl = href('/books/the-first-30-days-as-a-new-manager/');
const summaryUrl = href('/books/the-first-30-days-as-a-new-manager/summary/');
const articlesUrl = href('/articles/');
const ideas = [["Authority is a pattern, not a personality.", "New managers often try to perform authority. The better move is to repeat clear, calm signals until the team knows what to expect."], ["Overexplaining is usually anxiety.", "Long explanations may feel responsible, but they can make decisions sound unstable and negotiable."], ["Feedback should be narrow.", "Feedback becomes damaging when it carries delay, judgment, and emotional weight. Keep it factual and specific."], ["Delegation needs a finish line.", "A vague handoff creates anxiety and hovering. Define outcome, constraints, and checkpoint."], ["Silence is part of management.", "Not every gap needs to be filled with explanation, reassurance, or another meeting."], ["Week one is for observation.", "Trying to fix everything immediately usually signals anxiety. Listen, map, then choose one change."], ["Capacity is a management responsibility.", "Saying yes to everything does not make you helpful. It makes you unavailable for the work only you can do."], ["Former peers need clear containers.", "The relationship can stay warm, but the structure cannot stay the same after promotion."]];
const schema = { '@context': 'https://schema.org', '@type': 'Article', headline: 'Key Ideas from The First 30 Days as a New Manager', description: 'Core management ideas from the Plain Act book for first-time managers.', author: { '@type': 'Organization', name: 'Plain Act' } };
---
<BaseLayout title="Key Ideas from The First 30 Days as a New Manager | Plain Act" description="Core ideas from The First 30 Days as a New Manager: quiet authority, restraint, delegation, feedback, boundaries, and first-month pressure." path="/books/the-first-30-days-as-a-new-manager/key-ideas/" schema={[schema]}>
  <main class="plain-page"><header class="plain-hero"><p class="kicker">Key Ideas</p><h1>Key ideas from the book.</h1><p>The book is built around one practical question: how does a new manager stay clear, steady, and useful when the pressure of authority starts changing their behavior?</p><div class="actions"><a href={bookUrl}>Open the book page</a><a href={summaryUrl}>Read the summary</a><a href={articlesUrl}>Articles</a></div></header><section class="list-grid">{ideas.map(([title, text]) => (<article class="text-card"><h2>{title}</h2><p>{text}</p></article>))}</section></main>
</BaseLayout>

<style>
  .plain-page { max-width: 1040px; margin: 0 auto; padding: 3rem 1.2rem 4rem; }
  .plain-hero { max-width: 820px; margin-bottom: 2rem; }
  .kicker { margin: 0 0 0.65rem; color: var(--accent); font-size: 0.78rem; letter-spacing: 0.14em; text-transform: uppercase; font-weight: 700; }
  .plain-hero h1 { margin: 0; font-size: clamp(2.2rem, 5vw, 4.35rem); line-height: 0.96; letter-spacing: -0.06em; }
  .plain-hero p { margin-top: 1rem; color: var(--muted); font-size: 1.05rem; line-height: 1.7; }
  .actions { display: flex; flex-wrap: wrap; gap: 0.7rem; margin-top: 1.2rem; }
  .actions a, .text-card a { color: var(--text); text-decoration: none; border: 1px solid var(--border); border-radius: 999px; padding: 0.55rem 0.85rem; font-size: 0.9rem; background: rgba(255,255,255,.035); }
  .actions a:first-child { color: var(--accent); }
  .content-grid { display: grid; grid-template-columns: minmax(0, 1.4fr) minmax(260px, .6fr); gap: 1rem; align-items: start; }
  .main-card, .side-card, .text-card, .quote-card, .faq-card { border: 1px solid var(--border); border-radius: 1.15rem; background: rgba(255,255,255,.035); padding: 1.15rem; box-shadow: 0 24px 70px rgba(0,0,0,.16); }
  .main-card h2, .side-card h2, .text-card h2, .faq-card h2 { margin-top: 0; font-size: clamp(1.35rem, 2.5vw, 2rem); letter-spacing: -0.035em; }
  .main-card h3, .text-card h3 { margin-top: 1.55rem; margin-bottom: .45rem; }
  .main-card p, .main-card li, .side-card p, .side-card li, .text-card p, .text-card li, .faq-card p { color: var(--muted); line-height: 1.75; font-size: 0.98rem; }
  .main-card strong, .text-card strong { color: var(--text); }
  .list-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .9rem; }
  .text-card { min-height: 100%; }
  .quote-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .9rem; }
  .quote-card blockquote { margin: 0; color: var(--text); font-size: 1.08rem; line-height: 1.55; letter-spacing: -0.02em; }
  .quote-card p { color: var(--muted); margin-bottom: 0; font-size: .9rem; }
  .checklist { display: grid; gap: .75rem; }
  .check-item { border: 1px solid var(--border); border-radius: .95rem; padding: .9rem; background: rgba(255,255,255,.025); }
  .check-item strong { display: block; margin-bottom: .35rem; }
  .check-item p { margin: 0; }
  .faq-list { display: grid; gap: .9rem; }
  .faq-card h2 { margin-bottom: .35rem; }
  @media (max-width: 860px) { .content-grid, .list-grid, .quote-grid { grid-template-columns: 1fr; } .plain-page { padding-top: 2rem; } }
</style>
