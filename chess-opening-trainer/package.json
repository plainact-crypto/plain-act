{
  "name": "chess-opening-trainer",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "copy-assets": "node scripts/copy-assets.mjs",
    "inject-activation": "node scripts/inject-activation.mjs",
    "dev": "npm run copy-assets && npm run inject-activation && vite",
    "build": "npm run copy-assets && npm run inject-activation && vite build",
    "test": "node --test tests/*.test.js && node --check activation-onboarding-v2.js && node --check scripts/inject-activation.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "chess.js": "1.4.0",
    "cm-chessboard": "^8.7.8",
    "stockfish": "^18.0.8"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  },
  "allowScripts": {
    "stockfish@18.0.8": true
  }
}
