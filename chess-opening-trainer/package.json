{
  "name": "chess-opening-trainer",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "copy-assets": "node scripts/copy-assets.mjs",
    "inject-performance": "node scripts/inject-training-performance.mjs",
    "inject-activation": "node scripts/inject-activation.mjs",
    "inject-regression-root-fix": "node scripts/inject-regression-root-fix.mjs",
    "remove-depth-5": "node scripts/remove-depth-5.mjs",
    "migrate-player-repertoire": "node scripts/migrate-player-repertoire.mjs",
    "inject-training-lines": "node scripts/inject-training-lines.mjs",
    "dev": "npm run copy-assets && npm run inject-performance && npm run inject-activation && npm run inject-regression-root-fix && npm run remove-depth-5 && npm run migrate-player-repertoire && npm run inject-training-lines && vite",
    "build": "npm run copy-assets && npm run inject-performance && npm run inject-activation && npm run inject-regression-root-fix && npm run remove-depth-5 && npm run migrate-player-repertoire && npm run inject-training-lines && vite build",
    "test": "node --test tests/*.test.js && node --check training-lines-patch.js && node --check variation-depth-progression-patch.js && node --check scripts/inject-training-lines.mjs && node --check training-performance-audio-patch.js && node --check scripts/inject-training-performance.mjs && node --check activation-onboarding-v2.js && node --check scripts/inject-activation.mjs && node --check regression-root-fix.js && node --check scripts/inject-regression-root-fix.mjs && node --check scripts/remove-depth-5.mjs && node --check scripts/migrate-player-repertoire.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "chess.js": "1.4.0",
    "cm-chessboard": "^8.7.8",
    "stockfish": "^18.0.8"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  },
  "allowScripts": {
    "stockfish@18.0.8": true
  }
}
